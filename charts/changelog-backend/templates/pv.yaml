kind: PersistentVolume
apiVersion: v1
metadata:
  name: changelog-backend-pv-volume
  labels:
    type: local
spec:
  storageClassName: manual
  capacity:
    storage: 30Gi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: "/mnt/changelog-backend"
